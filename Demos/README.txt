This folder contains various data for testing the code.
For example, we have data in multiple different formats each format
being a specific way the data can be loaded from the hardware.

We also have an example case where the data is streamed off the
device (and thus not processed in bulk).

Jesper Kristensen, Sep 2019
